{
  "name": "flag-removal-map-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite --config site/vite.config.ts",
    "test": "cargo test --all-targets && cargo test --doc && npm run test:site && npm run test:browser",
    "test:site": "node --test site/tests/site.test.mjs",
    "test:browser": "npm run build:site && node --test site/tests/browser.test.mjs",
    "test:claims": "npm run test:site && npm run test:browser && cargo test --test cli",
    "lint": "cargo fmt --check && cargo clippy --all-targets -- -D warnings",
    "typecheck": "tsc --project site/tsconfig.json",
    "check": "npm run typecheck && npm run lint",
    "build": "npm run check && cargo build --release && npm run build:site",
    "build:site": "vite build --config site/vite.config.ts"
  },
  "devDependencies": {
    "axe-core": "4.11.0",
    "playwright": "1.58.2",
    "typescript": "5.9.3",
    "vite": "^7.1.3"
  }
}
